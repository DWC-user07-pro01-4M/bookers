<body>
    <p>
        <strong>Title:</strong>
            <%= Book.find(params[:id]).title %>
    </p>

    <p>
        <strong>Body:</strong>
            <%= @books.body %>
    </p>

        <p>
        <%= link_to "Edit", edit_book_path(@books.id) %>
        |
        <%= link_to "Back", books_path %>
        </p>
</body>